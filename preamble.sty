%\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{preamble}

% Preamble for doctoral thesis "A geometric realisation of affine 0-Schur algebras".
% Author: Tom Crawley, working at University of Bath.
% Supervisor: Dr Xiuping Su.
% Funding: EPSRC grant.

% -------------------------------------------- %
% 				PACKAGES						 %
% -------------------------------------------- %

\RequirePackage{amsmath}
\RequirePackage{amsfonts}
\RequirePackage{amsthm}
\RequirePackage{enumerate}
\RequirePackage{xcolor}
\RequirePackage{mathtools}	% included for use of coloneqq.

\usepackage{tikz-cd} % create (commuting) diagrams.
\usepackage{biblatex}

% LOAD BIBLIOGRAPHY
\addbibresource{references.bib}

% OTHER PACKAGES:

%\usepackage{extpfeil} 	% for interesting arrows.
%\usepackage{hyperref} 	% enable navigation hyperlinks

%\usepackage{import}
%For importing sections, bibliography etc.
%Usage: \import[path]{filename}

% -------------------------------------------- %
% 				EDITORIAL MACROS				 %
% -------------------------------------------- %

% marker to continue typing:
\newcommand{\seeyoulater}{ {\color{red}CONTINUE...} }

% proof reminder(s)
\newcommand{\provelater}{ {\color{red}INCLUDE A PROOF LATER.} }

\newcommand{\finishproof}{ {\color{red}COMPLETE THIS PROOF.} }

% -------------------------------------------- %
% 				STYLE AND FORMATTING			 %
% -------------------------------------------- %

\setlength{\oddsidemargin}{0pt}
\setlength{\textwidth}{463pt}
\setlength{\marginparsep}{0pt}
\setlength{\marginparwidth}{60pt}
\setlength{\topmargin}{20pt}
\setlength{\headheight}{0pt}
\setlength{\headsep}{0pt}
\setlength{\textheight}{650pt}
\setlength{\footskip}{20pt}


% -------------------------------------------- %
% 				THEOREM ENVIRONMENTS			 %
% -------------------------------------------- %

\newtheorem{theorem}{Theorem}[section]
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{corollary}[theorem]{Corollary}

\newtheorem{definition}{Definition}[section]

\newtheorem{remark}{Remark}
\newtheorem{example}{Example}
\newtheorem{observation}{Observation}

% -------------------------------------------- %
%			 MATHEMATICAL OPERATORS				 %
% -------------------------------------------- %

% (linear) span
\DeclareMathOperator{\SPAN}{span}

% morphism spaces
\DeclareMathOperator{\HOM}{Hom}

% endomorphism ring
\DeclareMathOperator{\END}{End}

% automorphism group
\DeclareMathOperator{\AUT}{Aut}

% extension groups
\DeclareMathOperator{\EXT}{Ext}

% image
\DeclareMathOperator{\IM}{im}%image

% --------------------------------------------- %
% 				NOTATION					 	  %
% --------------------------------------------- %

% COMMON MATHEMATICAL NOTATION
\newcommand{\integers}{\mathbb{Z}}
\newcommand{\complex}{\mathbb{C}}
\newcommand{\rationals}{\mathbb{Q}}
\newcommand{\naturals}{\mathbb{N}}
\newcommand{\field}{\mathbf{k}}

% GENERAL LINEAR GROUPS

\newcommand{\GL}[2]{\operatorname{GL}_{#1}(#2)}
% argument 1 denotes the rank while argument 2 denotes the ground ring

% GROUND RINGS FOR LATTICES:

\renewcommand{\epsilon}{\varepsilon}
% Laurent polynomial ring

\newcommand{\laurent}{{\field[\epsilon,\epsilon^{-1}]}}
% polynomial ring
\newcommand{\polys}{{\field[\epsilon]}}

% compact notation:
\renewcommand{\laurent}{\mathcal{S}}
\renewcommand{\polys}{\mathcal{R}}


% AFFINE FLAG VARIETIES [single, double, triple]
\newcommand{\flags}{\mathcal{F}}
\newcommand{\dblflags}{{\flags\!\times\!\flags}}
\newcommand{\triflags}{{\flags\!\times\!\flags\!\times\!\flags}}

% compositions (of r into n parts)
\newcommand{\compositions}[1][n,r]{\Lambda_0(#1)}

% characteristic matrix of a pair.
\newcommand{\type}[2]{\operatorname{A}(#1,#2)}

% periodic characteristic matrices.
\newcommand{\matrices}[1][n,r]{\Lambda_1(#1)}

% cyclic quivers
\newcommand{\cyclic}{\Delta}

% quiver for presentation of q schur algebras.
\newcommand{\presentationquiver}[1][n,r]{\Gamma(#1)}

% ideal of relations for the quiver presentation
\newcommand{\quiverrelations}[1][n,r]{I(#1)}


% VARIOUS SCHUR ALGEBRAS

% classical Schur algebras
\newcommand{\schur}[1][n,r]{S(#1)}
% classical affine Schur algebras
\newcommand{\affineschur}[1][n,r]{\hat{S}(#1)}
% quantum Schur algebras
\newcommand{\qschur}[1][n,r]{S_q(#1)}
% quantum affine Schur algebras
\newcommand{\affineqschur}[1][n,r]{\hat{S}_q(#1)}
% affine 0-Schur algebras
\newcommand{\affinezeroschur}[1][n,r]{\hat{S}_0(#1)}
% generic affine Schur algebras (realisation of 0-Schur algebras.)
\newcommand{\generic}[1][n,r]{\hat{G}(#1)}


% transpose operator
\newcommand{\transpose}{\top}
% other possibilities are \top, ...




% -------------------------------------------- %
%				TITLE PAGE						 %
% -------------------------------------------- %

\author{Tom Crawley} % with Xiuping Su.
\title{A geometric realisation of affine 0-Schur algebras.}
